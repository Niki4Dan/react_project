generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Idea {
  id          String   @id @default(uuid())
  ideaNick    String   @unique
  name        String
  description String
  text        String
  createdAt   DateTime @default(now())
}
